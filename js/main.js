function alertUser(msg) {
alert(msg);
}


/*
// if hover -> display txt
function displayTxt(evt) {
    evt.currentTarget.parentNode.querySelector( '.preview-title' ).classList.remove( 'hide' );
}
// if leave -> hide txt
function removeTxt(evt) {
    evt.currentTarget.querySelector( '.preview-title' ).classList.add( 'hide' );
}

/* mouseover and mouseout events to `.previewimage` element */
/*var $previewimage = document.querySelector( '.previewimage' );
$previewimage.addEventListener( 'mouseover', displayTxt );
$rowcol.addEventListener( 'mouseout', removeTxt );
*/
